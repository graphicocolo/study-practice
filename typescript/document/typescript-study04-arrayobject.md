# 課題：配列・オブジェクトの型定義

**目標：** 配列とオブジェクトに型をつける方法を覚える

---

## 準備

`study-practice/typescript/code/` に新しいファイルを作成：

```
04-array-object.ts
```

---

## やること

### ① 配列の型定義

```ts
// string の配列、number の配列をそれぞれ宣言する
// 書き方は2通りある（どちらでも正解）
// 例： string[]  または  Array<string>
```

### ② 配列に違う型を混ぜようとする

```ts
// string[] の配列に number を push しようとするとどうなるか確認する
```

### ③ オブジェクトの型定義

```ts
// 以下の情報を持つオブジェクトに型をつけて宣言する
// - id: number
// - name: string
// - isActive: boolean
```

### ④ type を使って型に名前をつける

```ts
// ③で書いた型を、type キーワードで名前をつけて定義し直す
// その型を使ってオブジェクトを2つ作る
```

### ⑤ オブジェクトの配列

```ts
// ④で作った型の配列を宣言して、オブジェクトを2〜3件入れる
// 配列をループして、name だけ console.log で出力する
```

---

## 確認ポイント

**④の `type` は前回の課題で出てきた型アノテーションとどう違うか**

type`type` は、型の使い回しが可能

**⑤のループには JavaScript で学んだどのメソッドが使えるか**

`forEach`